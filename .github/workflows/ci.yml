name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci
      - name: Link
        run: npm run lint
      - name: Expo App type check
        run: npx tsc --noEmit
      - name: Media Service type check
        run: cd backend/media-service && npm ci && npx tsc --noEmit
      - name: Lambda type check
        run: cd lambda && npm ci && npx tsc --noEmit
